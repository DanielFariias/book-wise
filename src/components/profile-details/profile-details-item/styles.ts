import { styled } from 'stitches.config'

export const ProfileDetailsItemContainer = styled('div', {
  display: 'flex',
  gap: '$5',

  svg: {
    width: 32,
    height: 32,
    color: '$green100',
  },
})
